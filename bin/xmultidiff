#!/usr/bin/python3

import sys

class MultiDiffTool:
    def process(self, argv):
        file1, file2 = argv[1:]
        self.processFiles(file, file2)

    def process(file1, file2):
        if os.path.isdir(file1):
        elif os.path.isfile(file1):
            if os.path.isdir(file2):
            elif: 
        else:
            self.error('unsupported filetype', file1)
        

if __name__ == '__main__':
    MultiDiffTool().process(sys.argv)
